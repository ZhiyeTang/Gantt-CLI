# Gantt CLI

Gantt CLI 协调需要隔离或协作的开发任务，并根据交付证据确认完成。

## Language

**轻量任务（Inline task）**：范围明确、低风险且可直接验证的工作，由 Agent 判断并简述理由，默认直接完成且不登记到 Gantt。文件数量不决定任务轻重。

**受管任务（Requirement）**：需要拆分协作、依赖协调或隔离实验，并由 Gantt 记录和跟踪交付的工作。

**执行尝试（Assignment）**：受管任务的一次执行；同一任务中断后可以重新安排执行尝试。

**协调计划（Coordination plan）**：多个任务修改重叠范围时，对修改分工、合并顺序和整体验证方式的明确安排。

**任务产物（Task artifact）**：为交付当前任务产生或修改、需要在完成前明确处置的内容。是否被版本控制跟踪，不决定它是否属于任务产物。

**本地文件（Local file）**：服务于本地环境、未作为任务交付内容的配置或其他文件；其归属需要已有记录或显式声明支持，不能只根据文件名推断。

**收口（Completion）**：将已提交的任务变更合并、验证、完成文件保全和执行现场清理，并记录任务完成的过程。
